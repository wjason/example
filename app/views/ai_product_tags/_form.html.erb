<%= form_with(model: ai_product_tag) do |form| %>
  <% if ai_product_tag.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ai_product_tag.errors.count, "error") %> prohibited this ai_product_tag from being saved:</h2>

      <ul>
        <% ai_product_tag.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :ai_product_id, style: "display: block" %>
    <%= form.number_field :ai_product_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
